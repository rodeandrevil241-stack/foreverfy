# Drop these `location` blocks inside your `server { ... }` to deny
# direct browser access to config/ and logs/. PHP must still be able
# to READ those paths from disk; this only blocks HTTP exposure.

location ^~ /config/ {
    deny all;
    return 403;
}

location ^~ /logs/ {
    deny all;
    return 403;
}
